/*
call stack
      call stack runs code inside it
      
   event loop
   It is a  mediator which continously monitor call stack and memory queue
   if call stack is empty,it pushes the function from memory queue to call stack




   why memory queue?
   
*/