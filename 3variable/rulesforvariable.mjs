let name1$="karuna"
//we can use all alphabet as variable
//all number can be used(but cannot be used at first forex:1name)
//no symbol can be use but _ and $ can be used
 

//while defining variable use camelcase convention(for ex:fullname as fullName)
let fullName="karuna subedi"

//variable name must be descriptive for example let name="karuna" instead of let a ="karuna"